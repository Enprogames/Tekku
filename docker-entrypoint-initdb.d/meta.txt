Scripts in this folder with the extensions ".sh", ".sql", etc will be executed
in alphabetical order when the container is first initialized.
To reinitialize, stop the container, delete it, then rebuild it.